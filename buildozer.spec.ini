specs_content = """
[app]
title = Dost Assistant
package.name = myassistant
package.domain = org.test
source.dir = .
source.include_exts = py,png,jpg,kv,atlas
version = 0.1
requirements = python3,kivy==2.2.1,plyer,pyjnius,requests

# Permissions
android.permissions = INTERNET, SYSTEM_ALERT_WINDOW, WAKE_LOCK, PACKAGE_USAGE_STATS, FOREGROUND_SERVICE, RECORD_AUDIO

# Service logic
services = Assistant:service.py

# Android API Settings (Updated)
android.api = 33
android.minapi = 21
android.sdk = 33
android.ndk = 25b
android.arch = armeabi-v7a
p4a.branch = master

[buildozer]
log_level = 2
warn_on_root = 1
"""

with open("buildozer.spec", "w") as f:
    f.write(specs_content.strip())
print("buildozer.spec update ho gayi hai!")